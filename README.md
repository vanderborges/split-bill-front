# Split Bill Front

Aplicativo Flutter para Android, iOS, Web, Windows, macOS e Linux.

Quando o Flutter SDK estiver instalado:

```bash
flutter create .
flutter pub get
flutter run
```

O `flutter create .` gera os diretorios nativos de plataforma sem sobrescrever o `lib/` existente.
