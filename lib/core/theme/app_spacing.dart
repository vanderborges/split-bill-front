/// Escala de espaçamento do Design System do DividiAí.
///
/// Usar sempre um desses valores em `padding`/`SizedBox`/`spacing` de telas
/// novas ou refeitas, em vez de números soltos.
class AppSpacing {
  const AppSpacing._();

  static const double xs = 4;
  static const double sm = 8;
  static const double md = 12;
  static const double lg = 16;
  static const double xl = 24;
  static const double xxl = 32;
}

/// Raios de borda do Design System do DividiAí.
class AppRadius {
  const AppRadius._();

  static const double card = 12;
  static const double sheet = 20;
  static const double pill = 999;
}
